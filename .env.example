# Environment Variables

# JWT Secret for token signing (REQUIRED - Generate with: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))")
# NEVER use default in production!
JWT_SECRET=your-super-secret-jwt-key-change-this-to-random-64-char-string

# JWT token expiration (optional, default: 8h)
JWT_EXPIRATION=8h

# Initial password for user 'kat' (CHANGE THIS!)
INITIAL_USER_PASSWORD=changeMe123!
